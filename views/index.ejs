<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Url-Shortener-Tarun</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-blue-100 to-purple-100 min-h-screen flex flex-col items-center py-10 font-sans">

    <h1 class="text-4xl font-bold text-purple-700 mb-8">🌐 URL Shortener</h1>

    <form action="/shorturls" method="post" class="bg-white shadow-lg rounded-lg p-6 w-full max-w-md mb-10">
        <label for="fullurl" class="block text-gray-700 font-semibold mb-2">Enter your URL</label>
        <input type="url" name="fullurl" id="fullurl" placeholder="https://example.com"
                class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-purple-400 mb-4" required />
        <button type="submit" 
                class="w-full bg-purple-600 text-white font-semibold py-2 rounded-md hover:bg-purple-700 transition">
            🔗 Shrink URL
        </button>
    </form>

    <div class="w-full max-w-4xl bg-white shadow-lg rounded-lg overflow-hidden">
        <table class="min-w-full text-left">
            <thead class="bg-purple-600 text-white">
                <tr>
                    <th class="px-6 py-3">Full URL</th>
                    <th class="px-6 py-3">Short URL</th>
                    <th class="px-6 py-3">Click Count</th>
                </tr>
            </thead>
            <tbody class="divide-y divide-gray-200">
                <% shorturls.forEach(shorturl=>{%>
                    <tr class="hover:bg-purple-50 transition">
                        <td class="px-6 py-4">
                            <a href="<%=shorturl.full%>" class="text-blue-600 hover:underline"><%=shorturl.full%></a>
                        </td>
                        <td class="px-6 py-4">
                            <a href="<%=shorturl.short%>" class="text-green-600 hover:underline"><%=shorturl.short%></a>
                        </td>
                        <td class="px-6 py-4">1<%=shorturl.clicks%></td>
                    </tr>
                <%}) %>
            </tbody>
        </table>
    </div>

</body>
</html>
